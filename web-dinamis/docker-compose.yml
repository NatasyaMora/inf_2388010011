# Layanan database
services:
  db-webdinamis:
    image: mariadb:lts
    environment:
      MARIADB_ROOT_PASSWORD: AnakBaik11
      MARIADB_DATABASE: dbcompro
      MARIADB_USER: user2webdinamis_2388010011
      MARIADB_PASSWORD: Natsya_2388
    volumes:
      - db_data:/var/lib/mysql
      - ./db_data/dbcompro_nim.sql:/docker-entrypoint-initdb.d/dbcompro_nim.sql:ro
    restart: always

  container-statis:
    image: natasyamora11/inf_2388010011:latest
    ports:
      - "80:80"
    restart: always

  container-dinamis:
    image: natasyamora11/web-dinamis:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      DB_USER: user2webdinamis_2388010011
      DB_PASSWORD: Natsya_2388
      DB_NAME: dbcompro
      DB_HOST: db-webdinamis
      NEXTAUTH_SECRET: ini-kata-kata-bebas-saja-32-karekter
      NEXTAUTH_URL: http://3.25.181.208:3000
    depends_on:
      - db-webdinamis

volumes:
  db_data: {}
